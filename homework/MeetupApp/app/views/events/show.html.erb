<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @event.title %>
  <b>Date:</b>
  <%= @event.date %>
  <b>Venue:</b>
  <%= Venue.find_by_id(@event.venue_id).name %>
</p>


<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
